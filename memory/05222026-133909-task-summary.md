# Task Complete: Implement Tools Catalog Service Layer

## Date
2026-05-22 13:39

## Task
Build CRUD operations for global tools.

## What Was Done

### `app/models.py` — Updated 
- Added `brand`, `model_number`, `is_active`, `merchant_id` columns to Tool model
- Added `Merchant.tools` relationship


### `app/services/tool_service.py` — Created
- `list_tools(active_only=None, category_id=None)` — list with active/inactive/category filters
- `get_tool(tool_id)` — get by ID
- `create_tool(name, default_price, brand, model_number, category_id, merchant_id, notes, is_active)` — create with validation
- `update_tool(tool_id, **kwargs)` — update any fields with validation
- `delete_tool(tool_id)` — hard delete by ID
- Validates: required name, price >= 0, valid FK references

### `schema.sql` — Updated
- Added `brand`, `model_number`, `is_active`, `merchant_id` to tools table DDL
- Added FK constraint for `merchant_id` referencing merchants
- Added `ix_tools_merchant_id` index

### `tests/test_tool_service.py` — Created 
- 8 tests covering all CRUD operations and validation edge cases
- All pass against in-memory SQLite

## Acceptance Criteria — All Met
- ✓ Supports fields: category, default price, brand, model number, merchant, notes, is_active
- ✓ List view can filter active/inactive items
- ✓ Validations protect required fields and sensible default prices (>= 0)
