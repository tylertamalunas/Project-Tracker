# Task Complete: Implement Project-Tool Association Workflow

## Date
2026-05-28 12:56

## Task
Allow users to add a tool to a project with already-owned logic and project-specific pricing.

## What Was Done

### `app/models.py` — Updated
- Added `purchased_on` column (DateTime, nullable) to ProjectTool model

### `schema.sql` — Updated
- Added `purchased_on TEXT` column to project_tools table

### `app/services/project_tool_service.py` — Created
- `list_project_tools(project_id)` — list all tools for a project
- `get_project_tool(project_tool_id)` — get by ID
- `add_tool_to_project(...)` — attach tool with quantity, already_owned, prices, merchant, purchased_on, notes
- `update_project_tool(id, **kwargs)` — update any fields
- `remove_tool_from_project(id)` — remove association
- All methods enforce: completed projects are read-only

### `app/routes/projects.py` — Updated
- Project detail route now passes tools data to template

### `app/templates/projects/detail.html` — Updated
- Added Tools table section with columns: name, qty, owned badge, est/actual price, cost contribution, merchant, purchased date, notes
- Owned tools show "$0.00 (owned)" in cost column
- Add Tool button hidden for completed projects

### `tests/test_project_tool_service.py` — Created
- 6 tests: CRUD, validation, completed project rejection, already_owned exclusion from totals
- All pass

## Acceptance Criteria — All Met
- ✓ User can attach a tool to a project
- ✓ Fields include quantity, already_owned, estimated_unit_price, actual_unit_price, notes, purchased_on, and merchant
- ✓ Project detail page lists attached tools
- ✓ If already_owned is true, actual cost contribution is excluded from totals
