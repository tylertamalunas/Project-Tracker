# Task Complete: Implement Projects Repository/Service Layer

## Date
2026-05-20 12:23

## Task
Build create/read/update/delete operations for projects.

## What Was Done

### `app/models.py` — Updated
- Added `budget_estimate` column (Numeric, nullable, optional manual budget target)
- Added `notes` column (Text, default empty)

### `app/services/project_service.py` — Created
- `list_projects(status_filter=None)` — returns all projects, optionally filtered by status
- `get_project(project_id)` — returns a single project by ID or None
- `create_project(name, description, status, start_date, end_date, budget_estimate, notes)` — creates and persists a new project with validation
- `update_project(project_id, **kwargs)` — updates any combination of fields with validation
- `delete_project(project_id)` — removes a project by ID
- Input validation: rejects empty names, invalid statuses
- Date parsing: accepts None, date objects, or ISO strings

### `schema.sql` — Updated
- Added `budget_estimate` and `notes` columns to projects table DDL

### `tests/test_project_service.py` — Created
- Tests for all 5 service methods
- Validation edge cases (empty name, invalid status, non-existent ID)
- All 7 tests pass

## Acceptance Criteria — All Met
- ✓ Supports fields: name, description, status, start_date, end_date, budget_estimate, notes
- ✓ Persists created_at and updated_at
- ✓ Methods exist for list, create, update, get by id, and delete
