# Task Complete: Implement Local Media Upload Storage Service

## Date
2026-06-02 11:28

## Task
Build local file handling for receipts, documents, and reference photos.

## What Was Done

### `app/services/media_service.py` — Created
- `upload_file(project_id, file, media_type, notes)` — saves file to disk + metadata to DB
- `list_project_media(project_id)` — list all media for a project
- `get_media(media_id)` — get by ID
- `get_media_full_path(media)` — resolve absolute filesystem path
- `delete_media(media_id)` — removes file from disk + record from DB
- Filename sanitization via `secure_filename` + UUID prefix
- Extension whitelist: png, jpg, jpeg, gif, pdf, doc, docx, txt
- Completed project protection

### Directory structure
- Files stored at: `uploads/<project_id>/<uuid>_<safe_filename>`
- Project-specific subdirectories created automatically

### `app/routes/media.py` — Created
- `GET/POST /projects/<id>/media/new` — upload form
- `GET /uploads/<path>` — serve files in development
- `GET /projects/<id>/media/<mid>/download` — download with original filename
- `POST /projects/<id>/media/<mid>/delete` — delete file + record

### `app/templates/media/upload.html` — Created
- File input with accepted extensions listed
- Media type dropdown (receipt, progress, document, other)
- Notes textarea

### `app/templates/projects/detail.html` — Updated
- Media table now has clickable filenames (links to view), Download button, Delete button
- Delete hidden for completed projects

### `app/routes/__init__.py` — Updated
- Registered `media_bp`

### `tests/test_media_service.py` — Created
- 8 tests: upload, filename normalization, extension validation, no file error, completed project rejection, listing, deletion (file + record), upload route
- All pass

## Acceptance Criteria — All Met
- ✓ Uploaded files saved in predictable local directory structure (`uploads/<project_id>/`)
- ✓ File metadata saved to `media` table
- ✓ Unsafe filenames normalized (secure_filename + UUID prefix)
- ✓ App can serve and download saved files in development
