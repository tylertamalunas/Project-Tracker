# Task Complete: Implement Project Totals and Variance Calculations

## Date
2026-05-28 13:03

## Task
Compute estimated total, actual total, and variance at the project level.

## What Was Done

### `app/services/cost_service.py` — Created
- `get_project_estimated_total(project_id)` — material + purchased tool estimated costs
- `get_project_actual_total(project_id)` — material + purchased tool actual costs
- `get_project_variance(project_id)` — actual - estimated
- `get_project_cost_summary(project_id)` — all metrics in one call (estimated, actual, variance, breakdowns by material/tool)
- Internal helpers: `_sum_material_estimated`, `_sum_material_actual`, `_sum_tool_estimated`, `_sum_tool_actual`

### `app/models.py` — Updated
- Refactored `Project.estimated_total`, `Project.actual_total`, `Project.variance` properties to delegate to `cost_service` helper functions
- Properties remain for template convenience, but calculation logic now lives in the service layer

### `tests/test_cost_service.py` — Created
- 8 tests covering: empty project, material totals, purchased tools, owned tools excluded, NULL actual = $0, combined totals, model/service consistency, non-existent project error
- All pass

### Verification
- All existing tests (project_material_service, project_tool_service) continue to pass after refactoring

## Acceptance Criteria — All Met
- ✓ Material total = sum(quantity * estimated/actual unit price)
- ✓ Tool total counts only purchased tools (already_owned = false)
- ✓ Project detail page shows estimated total, actual total, and variance (existing template unchanged)
- ✓ Calculation logic lives in one clear service layer (`cost_service.py`)
