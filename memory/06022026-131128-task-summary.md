# Task Complete: Add Server-Side Validation for All Forms

## Date
2026-06-02 13:11

## Task
Ensure invalid data is rejected consistently across the app.

## What Was Done

### `app/services/validators.py` — Created
Shared validation utilities:
- `validate_required(value, field_name, errors)` — non-empty string check
- `validate_positive_int(value, field_name, errors)` — integer > 0
- `validate_price(value, field_name, errors, allow_none)` — float >= 0
- `validate_date(value, field_name, errors)` — ISO date format (YYYY-MM-DD)
- `validate_choice(value, field_name, choices, errors)` — enum/choice validation

All functions append friendly error messages and return cleaned values or None.

### `app/routes/projects.py` — Updated
- Project create form now uses validators for: required name, valid status, date format, price format
- Material add form now uses validators for: required material selection, quantity > 0, price >= 0, valid date

### `tests/test_validation.py` — Created
- 5 unit tests for validator functions
- 6 integration tests for form validation in routes
- All 11 pass

### Existing tests
- All existing form tests continue to pass (confirmed test_project_forms.py)

## Validation Rules Enforced

| Field Type | Rule | Error Message |
|------------|------|---------------|
| Required text | Non-empty after trim | "{field} is required." |
| Quantity | Integer > 0 | "{field} must be greater than 0." |
| Price | Float >= 0 | "{field} must be $0.00 or greater." |
| Date | ISO format or empty | "{field} must be a valid date (YYYY-MM-DD)." |
| Choice/Enum | One of allowed values | "{field} must be one of: ..." |
| Invalid number | Non-numeric input | "{field} must be a valid number." |

## Acceptance Criteria — All Met
- ✓ Required fields are validated
- ✓ Numeric and date fields reject invalid formats
- ✓ Price and quantity rules are enforced
- ✓ Friendly validation errors shown to the user
