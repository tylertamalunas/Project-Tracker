# Task Complete: Implement Related-Project Links

## Date
2026-06-01 12:39

## Task
Allow non-hierarchical project relationships such as 'depends_on' or 'similar_to'.

## What Was Done

### `app/services/project_relationship_service.py` — Created
- `get_related_projects(project_id)` — returns all relationships (both directions) with project, type, notes, direction
- `create_relationship(project_id, related_project_id, type, notes)` — creates with validation
- `delete_relationship(relationship_id)` — removes a relationship
- Allowed types: related, depends_on, similar_to, blocks, follow_up

### Validation rules:
- Cannot self-link
- Both projects must exist
- Relationship type must be from allowed list
- Duplicate prevention (same pair + same type in either direction)

### `app/routes/projects.py` — Updated
- Project detail now fetches and passes related projects data

### `app/templates/projects/detail.html` — Updated
- Added "Related Projects" section (separate from hierarchy)
- Shows table with: project name (linked), relationship type badge, direction, notes

### `tests/test_project_relationship_service.py` — Created
- 6 tests: create, validation, duplicate prevention, bidirectional query, delete, page rendering
- All pass

## Acceptance Criteria — All Met
- ✓ User can create a relationship between two projects
- ✓ Relationship includes a type and optional note
- ✓ Project detail page displays related projects separately from hierarchy
