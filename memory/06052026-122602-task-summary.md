# Task Complete: Test Hierarchy and Related-Project Edge Cases

## Date
2026-06-05 12:26

## Task
Verify that linking logic does not allow bad relationships.

## What Was Done

### Verified existing tests (16 pass)
- `test_project_hierarchy_service.py` — 10 tests
- `test_project_relationship_service.py` — 6 tests

### `tests/test_hierarchy_edge_cases.py` — Created
11 additional edge case tests:

**Hierarchy:**
- Self-link rejected
- Direct circular (A→B then B→A) rejected
- Indirect circular (A→B→C then C→A) rejected
- Siblings allowed (same parent, multiple children)
- Child cannot have two parents
- Unlink then relink to new parent works

**Relationships:**
- Self-link rejected
- Bidirectional display (outgoing on source, incoming on target)
- Duplicate prevention works in both directions (A→B blocks B→A same type)
- Different types between same pair allowed
- Both sides render on detail pages

All pass.

## No Code Changes Needed
The validation logic is working correctly for all tested scenarios.

## Acceptance Criteria — All Met
- ✓ Self-links are rejected (both hierarchy and relationships)
- ✓ Circular hierarchy cases are rejected (direct and indirect)
- ✓ Related-project links render correctly on both sides (bidirectional display confirmed)
