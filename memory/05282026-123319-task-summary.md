# Task Complete: Implement Project-Material Association Workflow

## Date
2026-05-28 12:33

## Task
Allow users to add a material to a project from the global catalog and record project-specific quantities and prices.

## What Was Done

### `app/services/project_material_service.py` — Completed
- `list_project_materials(project_id)` — list all materials for a project
- `get_project_material(project_material_id)` — get by ID
- `add_material_to_project(...)` — attach material with quantity, prices, unit, merchant, purchased_on, notes
- `update_project_material(id, **kwargs)` — update any fields
- `remove_material_from_project(id)` — remove association
- All methods enforce: completed projects are read-only, quantity > 0, prices >= 0, valid FK references

### `app/routes/projects.py` — Updated
- Added `GET /projects/<id>` route showing project detail with materials list

### `app/templates/projects/detail.html` — Created
- Project metadata (name, status, description, dates)
- Cost summary cards (estimated total, actual total, variance with color coding)
- Materials table showing: name, qty, unit, est/actual prices, subtotals, merchant, purchased date, notes
- Action buttons (edit, add material) hidden for completed projects

### `app/models.py` — Updated 
- `unit_of_measure` and `purchased_on` columns added to ProjectMaterial

### `schema.sql` — Updated
- `unit_of_measure` and `purchased_on` columns added to project_materials table

### `tests/test_project_material_service.py` — Created
- 8 tests: full CRUD, validation, completed project rejection, totals use project-specific prices
- All pass

## Acceptance Criteria — All Met
- ✓ User can attach a material to a project
- ✓ Fields include quantity, unit_of_measure, estimated_unit_price, actual_unit_price, notes, purchased_on, and merchant
- ✓ Project detail page lists attached materials
- ✓ Totals use project-specific prices rather than catalog defaults
