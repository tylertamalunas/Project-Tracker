# Task Complete: Implement Material Categories CRUD

## Date
2026-05-27 08:04

## Task
Add material category management for the global catalog.

## What Was Done

### `app/services/material_category_service.py` — Created 
- `list_material_categories()` — return all ordered by name
- `get_material_category(material_category_id)` — get by ID
- `create_material_category(name, description)` — create with name validation and uniqueness check
- `update_material_category(material_category_id, **kwargs)` — update name/description with uniqueness enforcement
- `delete_material_category(material_category_id)` — delete (materials get category_id set to NULL)

### `tests/test_material_category_service.py` — Created 
- 8 tests: CRUD, duplicate name validation (case-insensitive), and FK nullification on delete
- All pass against in-memory SQLite

## Acceptance Criteria — All Met
- ✓ CRUD exists for category name and description
- ✓ Materials can reference a category (existing FK relationship)
- ✓ Validation prevents duplicate category names (case-insensitive check + DB UNIQUE constraint)
