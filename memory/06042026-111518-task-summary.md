# Task Complete: Project CRUD and Status Transitions

## Date
2026-06-04 11:15

## Task
Test and fix project CRUD and status behavior.

## Issues Fixed

### 1. Completed projects could not be edited
- **Before:** Edit route redirected away with "read-only" message for completed projects
- **After:** All projects can be edited regardless of status. Status can be changed back to "active" or "planned" from the edit form.

### 2. No way to delete projects
- **Before:** No delete route existed
- **After:** `POST /projects/<id>/delete` deletes any project regardless of status. Cascades to materials, tools, and media via FK constraints.

### 3. Flash messages not visible globally
- **Before:** Flash messages only showed on pages that had inline flash handling
- **After:** Added flash message display to `base.html` so all pages show success/error messages

## Changes Made

### `app/routes/projects.py`
- Removed completed-project block from edit route
- Added `POST /projects/<id>/delete` route

### `app/templates/projects/detail.html`
- Edit button now always visible
- Added Delete button (with confirmation dialog) for all statuses
- Removed duplicate flash handling (now in base)

### `app/templates/projects/list.html`
- Edit button now visible for completed projects too

### `app/templates/base.html`
- Added global flash message display (dismissible alerts)

### `tests/test_project_crud_status.py` — Created
- 8 tests: edit completed, reopen to planned, delete any status, cascade delete, non-existent delete, edit button visibility, delete button visibility, dates/budget persistence
- All pass

## Acceptance Criteria — All Met
- ✓ Manual/automated tests cover happy path and validation failures
- ✓ Dates and budget fields behave correctly
- ✓ Completed projects render correctly and can be reopened/deleted
