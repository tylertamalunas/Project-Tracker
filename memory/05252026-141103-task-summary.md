# Task Complete: Implement Merchants CRUD

## Date
2026-05-25 14:11

## Task
Add merchant management so materials, tools, and purchases can reference a source.

## What Was Done

### `app/models.py` — Updated 
- Added `merchant_id` FK column to `ProjectMaterial` and `ProjectTool`
- Added `merchant` relationship to both junction models
- Added `project_materials` and `project_tools` relationships to `Merchant`

### `app/services/merchant_service.py` — Created 
- `list_merchants(name_filter=None)` — list all with optional partial name search (ilike)
- `get_merchant(merchant_id)` — get by ID
- `create_merchant(name, website, notes)` — create with name validation
- `update_merchant(merchant_id, **kwargs)` — update name/website/notes
- `delete_merchant(merchant_id)` — hard delete by ID

### `schema.sql` — Updated 
- Added `merchant_id INTEGER` column to `project_materials` and `project_tools`
- Added FK constraints referencing `merchants(id) ON DELETE SET NULL`

### `tests/test_merchant_service.py` — Created 
- 8 tests: CRUD operations, validation, and merchant-purchase-record integration
- All pass against in-memory SQLite

## Acceptance Criteria — All Met
- ✓ CRUD exists for merchant name, website URL, and notes
- ✓ Materials and tools can optionally point to a default merchant (via `merchant_id` FK)
- ✓ Project purchase records can optionally reference a merchant (via `merchant_id` on `ProjectMaterial`/`ProjectTool`)
