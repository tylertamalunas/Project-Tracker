# Task Complete: Implement Materials Catalog Service Layer

## Date
2026-05-21 12:27

## Task
Build CRUD operations for global materials.

## What Was Done

### `app/models.py` — Updated
- Added `sku` column (String, optional SKU/part number)
- Added `brand` column (String, optional brand name)
- Added `is_active` column (Boolean, defaults True, for soft-deactivation)

### `app/services/material_service.py` — Created
- `list_materials(active_only=None, category_id=None)` — list with active/inactive/category filters
- `get_material(material_id)` — get by ID
- `create_material(name, default_price, unit_of_measure, sku, brand, category_id, merchant_id, notes, is_active)` — create with validation
- `update_material(material_id, **kwargs)` — update any fields with validation
- `delete_material(material_id)` — hard delete by ID
- Validates: required name, price >= 0, valid FK references for category and merchant

### `schema.sql` — Updated
- Added `sku`, `brand`, and `is_active` columns to materials table DDL

### `tests/test_material_service.py` — Created
- 8 tests covering all CRUD operations and validation edge cases
- All pass against in-memory SQLite

### Database
- Recreated `tracker.db` to include new columns
- Re-seeded with `python seed.py`

## Acceptance Criteria — All Met
- ✓ Supports fields: category, unit, default price, sku, brand, merchant, notes, is_active
- ✓ List view can filter active/inactive items
- ✓ Validations protect required fields and sensible default prices (>= 0)
