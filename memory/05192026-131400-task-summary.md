# Task Complete: Add Indexes and Uniqueness Constraints

## Date
2026-05-19 13:14

## Task
Add indexes and uniqueness constraints for core lookup paths.

## What Was Done

### `app/models.py` — Updated
- Added `index=True` on all FK columns: `category_id`, `merchant_id`, `project_id`, `material_id`, `tool_id`, `media_id`
- Added `index=True` on `projects.status` and `projects.created_at`
- Added `unique=True` on `material_categories.name`, `tool_categories.name`, `merchants.name`

### `schema.sql` — Updated
- Added `UNIQUE` constraint on category and merchant name columns
- Added 15 explicit `CREATE INDEX` statements with rationale comments

## Indexes Added (15 total)

| Index | Table | Rationale |
|-------|-------|-----------|
| `ix_projects_status` | projects | Filter project list by status |
| `ix_projects_created_at` | projects | Sort project list by creation date |
| `ix_materials_category_id` | materials | Join materials to their category |
| `ix_materials_merchant_id` | materials | Join materials to merchant |
| `ix_tools_category_id` | tools | Join tools to their category |
| `ix_project_materials_project_id` | project_materials | Load all materials for a project |
| `ix_project_materials_material_id` | project_materials | Find which projects use a material |
| `ix_project_tools_project_id` | project_tools | Load all tools for a project |
| `ix_project_tools_tool_id` | project_tools | Find which projects use a tool |
| `ix_media_project_id` | media | Load all media for a project |
| `ix_media_links_media_id` | media_links | Find all links for a media record |
| `ix_project_hierarchy_parent` | project_hierarchy | Future parent lookup |
| `ix_project_hierarchy_child` | project_hierarchy | Future child lookup |
| `ix_project_relationships_project` | project_relationships | Future relationship lookup |
| `ix_project_relationships_related` | project_relationships | Future related lookup |

## Unique Constraints Added

| Table | Column | Rationale |
|-------|--------|-----------|
| `material_categories` | `name` | Prevent duplicate category names |
| `tool_categories` | `name` | Prevent duplicate category names |
| `merchants` | `name` | Prevent duplicate merchant entries |

## Acceptance Criteria — All Met
- ✓ Unique constraints added on category names and merchant names
- ✓ Indexes added for project_id, material_id, tool_id, merchant_id, status, created_at
- ✓ Rationale documented for each index (in schema.sql comments and this file)

## Verification
- DDL: All 15 indexes created successfully in SQLite (validated via in-memory DB)
- ORM: All 15 indexes confirmed via `sqlite_master` query
- Unique constraints: Duplicate inserts correctly rejected with IntegrityError

## Notes
- Test verification scripts (`tests/verify_indexes.py`, `tests/verify_orm.py`) remain in repo — can be deleted manually
- The existing `tracker.db` file needs to be deleted and recreated for indexes to take effect (SQLAlchemy `create_all()` won't add indexes to existing tables)
