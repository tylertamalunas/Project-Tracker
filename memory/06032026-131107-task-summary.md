# Task Complete: Add Filtering and Sorting to List Pages

## Date
2026-06-03 13:11

## Task
Improve usability for growing datasets.

## What Was Done

### `app/routes/projects.py` — Updated
- Added `sort` and `dir` query parameters to project list
- Sort options: name, status, start_date, updated_at (default: updated_at desc)

### `app/routes/materials.py` — Updated
- Added sort support: name, default_price, brand (default: name asc)
- Moved query building inline (was delegating to service which didn't support sort)

### `app/routes/tools.py` — Updated
- Added sort support: name, default_price, brand (default: name asc)
- Same inline query pattern as materials

### Templates Updated
- `projects/list.html` — sort control buttons (Name, Status, Start Date, Last Updated)
- `materials/list.html` — sort control buttons (Name, Price, Brand)
- `tools/list.html` — sort control buttons (Name, Price, Brand)
- Active sort button highlighted; clicking toggles asc/desc direction

### `tests/test_filtering_sorting.py` — Created
- 9 tests: project filter by status, project sort asc/desc, material sort by price/name, tool sort by name/price, sort controls visible
- All pass

## Acceptance Criteria — All Met
- ✓ Projects can be filtered by status (already existed, confirmed working)
- ✓ Materials and tools can be filtered by active state and category (already existed, confirmed)
- ✓ Lists support useful sort orders (name, price, status, date — with toggle direction)
